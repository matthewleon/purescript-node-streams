{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "eslint src && pulp build -- --censor-lib --strict",
    "example:build": "eslint example && pulp build -I example -- --censor-lib --strict",
    "example:gzip": "eslint example && pulp run -I example -m Gzip",
    "example:yes": "eslint example && pulp run -I example -m Yes",
    "example:count": "eslint example && pulp run -I example -m Count",
    "test": "pulp test"
  },
  "devDependencies": {
    "eslint": "^4.11.0",
    "pulp": "^12.0.1",
    "purescript-psa": "^0.5.1",
    "rimraf": "^2.6.2",
    "stream-buffers": "^3.0.1"
  }
}
